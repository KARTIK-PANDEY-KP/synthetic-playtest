node_modules/
dist/
build/
.next/
out/
runs/
*.log
.DS_Store

# secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
codex-home/
.modal.toml

# spike artifacts (regenerated each run)
spikes/**/fixture.png
spike-answer-key.json

# nested lockfiles/workspaces confuse the monorepo root
apps/*/pnpm-lock.yaml
apps/*/pnpm-workspace.yaml
packages/*/pnpm-lock.yaml
spikes/*/pnpm-lock.yaml

# agent worktrees live inside the repo tree
.claude/
